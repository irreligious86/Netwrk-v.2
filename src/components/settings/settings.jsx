import React from 'react';
import classes from './settings.module.css';

const Settings = (props) => {
    return (
        <div className={classes['settings']}>
            <h5 className={classes['settings-title']}>Settings</h5>

        </div>
    )
}

export default Settings;